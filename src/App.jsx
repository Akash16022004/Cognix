// Legacy App component is no longer used after routing refactor.
// Keeping the file to avoid breaking imports, but exporting a simple stub.

import React from 'react';

function App() {
  return null;
}

export default App;
